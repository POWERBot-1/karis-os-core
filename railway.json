{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python3 -m src.db.migrator --migrate && python3 -m src.db.seed_data --seed && uvicorn src.api.main:app --host 0.0.0.0 --port $PORT --workers 4",
    "healthcheckPath": "/docs",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
